#include <avr/io.h>
#define F_CPU 16000000
#define BAUD 9600
#define UBRR ((F_CPU/16/BAUD)-1)
//#include<avr/interrupt.h>
//#include<util/delay.h>

#include"device_init.h"

     
char a[23]={0x7E, 0x00, 0x13, 0x10, 0x01, 0x00, 0x13, 0xA2, 0x00, 0x40, 0xA2, 0x09, 0xA3, 0xFF, 0xFE, 0x00, 0x00, 0x68, 0x65, 0x6C, 0x6C, 0x6F, 0x9A};
char b[23]={0x7E, 0x00, 0x13, 0x10, 0x01, 0x00, 0x13, 0xA2, 0x00, 0x40, 0xA2, 0x09, 0xA3, 0xFF, 0xFE, 0x00, 0x00, 0x31, 0xD1};

void setup() {
 // _delay_ms(10000);
 device_init();
  //sei();
   
}
int i;
void loop() {
  
 for(i=0;i<23;i++)
 {
 _delay_ms(10);
   while((!(UCSR0A & (1<<UDRE0))))
  ;
    UDR0 = a[i];
    _delay_ms(10);
  /*  DDRB|=(1<<PB5);
  PORTB|=(1<<PB5);
  _delay_ms(100);
  PORTB&=~(1<<PB5);
  _delay_ms(100);*/
  
 }
}
